using UnityEngine;

/// <summary>A trigger volume that pushes the player. Generated levels use this instead of the WindLeft/WindRight tags.</summary>
public class CaveWind : MonoBehaviour
{
    public Vector2 force = new Vector2(1.5f, 0f);
}
